name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    run: |
      uses: ad-m/github-push-action@master
    with:
        github_token: ${{ secrets.GITHUB_TOKEN }}    
        
