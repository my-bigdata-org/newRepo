name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Commit files
      run: |
        git status
        git push origin HEAD:master --follow-tags --force --tags
        
